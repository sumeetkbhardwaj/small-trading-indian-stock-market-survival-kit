{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__.*|.*(?:order|gtt|position).*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/deny_write_tools.py\""
          }
        ]
      }
    ]
  }
}
