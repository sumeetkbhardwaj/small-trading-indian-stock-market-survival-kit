---
description: Deep, cited, survival-first research brief on a stock or theme
---
Run the `research` skill on $ARGUMENTS (an NSE/BSE symbol, or a theme/sector). Produce a cited, survival-first brief — **risks and red-flags first**, then business quality, valuation context, and technical/regime context — using free public data (screener + Yahoo delayed) with every figure stamped `{source, as_of, delay_class}`. If a position is being considered, route entry/stop/size/target through the deterministic gate via the capability resolver (Gate MCP → `scripts/kit.py` → JS-paise Artifact → refuse). Where the surface supports it, render a beautiful research Artifact. End with the disclaimer. Read-only; never place an order.
